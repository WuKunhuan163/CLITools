#!/bin/bash
# FILE_SELECT Command Script
# File Selection Tool - opens tkinter file selection dialog

# Get script directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Call Python version of FILE_SELECT script
exec /usr/bin/python3 "$SCRIPT_DIR/FILE_SELECT.py" "$@" 