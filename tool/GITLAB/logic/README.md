# GITLAB Logic

GitLab integration — repos, MRs, CI/CD. Logic layer contains only the MCP server configuration.

## Structure

| Module | Purpose |
|--------|---------|
| `mcp/main.py` | `MCPToolConfig` — MCP server registration, package type, capabilities, env vars |
