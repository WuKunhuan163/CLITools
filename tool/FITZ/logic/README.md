# FITZ Logic

PyMuPDF (fitz) wrapper for PDF operations. Suppresses noisy C-level stderr output from the mupdf library.

## Structure

| Module | Purpose |
|--------|---------|
| `pdf/wrapper.py` | `FitzWrapper` — stderr-suppressing wrapper around PyMuPDF |
