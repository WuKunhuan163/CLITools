# SHOTCUT

CLI harness for Shotcut - Video editing and rendering via melt/ffmpeg. Requires: melt (apt install melt), ffmpeg (apt install ffmpeg)

Migrated from [CLI-Anything/shotcut](https://github.com/HKUDS/CLI-Anything/tree/main/shotcut).

**Status**: Draft — needs post-processing for ecosystem integration.
