# DRAW Logic

Image drawing and annotation. Provides interfaces for drawing shapes, labels, and legends on images.

## Structure

| Module | Purpose |
|--------|---------|
| `engine.py` | Drawing functions — rectangles with alpha, labels, legends on PIL images |
