# SUNO Logic

AI music generation via Suno API. Logic layer contains only the MCP server configuration.

## Structure

| Module | Purpose |
|--------|---------|
| `mcp/main.py` | `MCPToolConfig` — MCP server registration, package type, capabilities, env vars |
