# GIT Logic

Git operations and GitHub API interactions. Provides programmatic access to git commands and GitHub REST API.

## Structure

| Module | Purpose |
|--------|---------|
| `engine.py` | `GitEngine` — git command execution, branch ops, GitHub API with rate limit handling |
