# GOOGLE.GDS Skills — Agent Guide

Skills specific to Google Colab / GDS remote development.

## When to use

- **GDS commands failing**: Use `gcs-raw-debug` to inspect the remote environment directly.

## Navigation

- Up: `tool/GOOGLE.GDS/` for tool overview
- Across: `skills/` at project root for general development skills
