# ZAPIER Logic

Workflow automation via Zapier MCP — access 8000+ apps. Logic layer contains only the MCP server configuration.

## Structure

| Module | Purpose |
|--------|---------|
| `mcp/main.py` | `MCPToolConfig` — MCP server registration, package type, capabilities, env vars |
