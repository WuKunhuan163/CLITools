# VPN Logic

VPN connection management. Controls VPN client binary, configuration, and connection lifecycle.

## Structure

| Module | Purpose |
|--------|---------|
| `engine.py` | `VpnEngine` — start/stop VPN, manage config, monitor connection |
