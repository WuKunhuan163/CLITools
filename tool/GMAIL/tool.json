{
  "name": "GMAIL",
  "version": "2.0.0",
  "description": "Gmail email client via official Gmail REST API (OAuth2)",
  "purpose": "Read inbox, search emails, send emails, manage labels and messages via the Gmail API with OAuth2 authentication.",
  "dependencies": [
    "PYTHON",
    "GOOGLE.CDMCP"
  ],
  "pip_dependencies": [
    "websocket-client"
  ]
}
