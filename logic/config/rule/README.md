# logic/config/rule

AI rule generation and management. Creates `.cursor/rules/` files and manages the auto-generated `AITerminalTools.mdc` rule.

## Structure

| Module | Purpose |
|--------|---------|
| `manager.py` | `RuleManager` — generate, update, and format AI agent rules from tool metadata |
