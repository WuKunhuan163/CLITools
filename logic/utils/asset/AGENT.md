# logic/asset - Agent Reference

## Key Interfaces

### audio/bell.mp3
- Static audio file; path: `logic/asset/audio/bell.mp3`
- Use `Path(__file__).parent / "audio" / "bell.mp3"` from asset module or resolve via project root

## Usage

Resolve path via `find_project_root` or `logic.utils.get_logic_dir` + `asset/audio/bell.mp3`.
