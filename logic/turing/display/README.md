# logic/turing/display

Terminal display management for the Turing Machine progress system. Handles width-aware text truncation, multi-line erasable output, and RTL support.

## Structure

| Module | Purpose |
|--------|---------|
| `manager.py` | `truncate_to_width()`, `_get_configured_width()`, `MultiLineManager` |
