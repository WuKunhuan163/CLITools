#!/bin/bash
# EXTRACT_PDF 命令脚本
# Enhanced PDF extraction using MinerU - 现在调用Python版本

# 获取脚本所在目录
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# 调用Python版本的EXTRACT_PDF脚本
exec /usr/bin/python3 "$SCRIPT_DIR/EXTRACT_PDF.py" "$@"
