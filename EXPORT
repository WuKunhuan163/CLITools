#!/bin/bash
# EXPORT Command Script
# Environment Variable Export Tool - now calls Python version

# Get script directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Call Python version of EXPORT script
exec /usr/bin/python3 "$SCRIPT_DIR/EXPORT.py" "$@" 