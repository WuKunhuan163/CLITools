#!/bin/bash
# RUN - Alias for RUN.py
# 为了向后兼容，将原来的bash脚本转换为Python脚本的别名

# 获取脚本所在目录
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# 调用Python版本的RUN脚本
exec python3 "$SCRIPT_DIR/RUN.py" "$@" 