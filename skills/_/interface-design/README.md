# Interface Design

When and how to extract interfaces from internal logic. Covers the facade layer pattern (`interface/main.py`), anti-patterns that signal extraction is needed, and documentation standards for interface contracts.

Read `SKILL.md` for the full guide.
