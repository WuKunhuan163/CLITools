# interface-design — Agent Guide

Teaches when to extract interfaces and how to design them correctly. Read SKILL.md for the full guide.

**Key signals for extraction:** cross-tool `logic/` imports, tool implementing another tool's function, too much implementation in `interface/`, duplicated utilities across tools.

**Going up:** `skills/_/` contains all foundational principles.
