#!/bin/bash
# DOWNLOAD Command Script
# Resource Download Tool - now calls Python version

# Get script directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Call Python version of DOWNLOAD script
exec /usr/bin/python3 "$SCRIPT_DIR/DOWNLOAD.py" "$@" 