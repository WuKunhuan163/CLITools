#!/bin/bash
# ALIAS Command Script
# Permanent Shell Alias Creation Tool - now calls Python version

# Get script directory
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Call Python version of ALIAS script
exec /usr/bin/python3 "$SCRIPT_DIR/ALIAS.py" "$@" 