# runtime/_/ — Agent Guide

Symmetric command runtime state. Git-tracked.

- `eco/brain/` — Brain context, tasks, sessions, exports. Updated via `BRAIN` CLI.
- `eco/experience/` — Lesson store, tool experience, daily logs. Updated via `SKILLS learn`.

Convention: `logic/_/<group>/` code reads/writes `runtime/_/<group>/` state.
