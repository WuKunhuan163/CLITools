#!/bin/bash
# EXTRACT_IMG 命令脚本
# 图片分析工具，现在调用Python版本

# 获取脚本所在目录
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# 调用Python版本的EXTRACT_IMG脚本
exec /usr/bin/python3 "$SCRIPT_DIR/EXTRACT_IMG.py" "$@"
